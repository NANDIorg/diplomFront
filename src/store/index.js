import { createStore } from 'vuex'
import * as user from '@/store/modules/user'
import * as dispute from '@/store/modules/dispute'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    user,
    dispute
  }
})
